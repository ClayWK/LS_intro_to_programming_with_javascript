function filter(input) {
  if (Array.isArray(input)) {
    // do something
  } else {
    // do something else
  }
}

// Use the Array.isArray(value) static method, which returns true if the value passed as an argument
// is an array.